mutation Login($email: String!, $password: String!) {
  loginViaEmail(input: {email: $email, password: $password}) {
    accessToken
    refreshToken
  }
}

mutation RefreshToken($refreshToken: String!) {
  refreshToken(refreshToken: $refreshToken) {
    accessToken
    refreshToken
  }
}